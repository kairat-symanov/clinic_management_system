<div class="col-sm-12">
  <div class="row">
    <div class="col-sm-12">
      <h1 class="title">Услуги</h1>
    </div>
    <div class="col-sm-3 col-inner-left">
      <div class="center-services">
        <div class="center-services-header">
      <span class="services-header-title">
        <i class="bi bi-grid-3x3-gap"></i>
        </span>Услуги
          <div class="services-content">
            <ul class="organization-services-list">
              <% @categories.each do |category| %>
                <tr>
                  <li class="active"><%= link_to category.title, category_path(category) %></li>
                </tr>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
  <%= link_to 'Создать услугу', new_category_path, class: 'btn btn-primary' %>
</div>

